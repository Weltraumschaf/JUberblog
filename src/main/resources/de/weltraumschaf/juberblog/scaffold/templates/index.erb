<h2>All Blog Posts</h2>
<ul>
    <% for post in posts %>
        <li>
            <a href="<%= post.url %>"><%= post.title %></a>
            <span class="meta">(<%= post.date_formatted %>)</span>
        </li>
    <% end %>
</ul>