(function() {
    function main(options) {
        // Put your JavaScript here.
    }

    function juberblog(opt) {
        options = opt || {};
        main(options);
    }

    window.juberblog = juberblog; // backward compatibility
})();